succeed [] or such base cases should go *after* inductive cases 
debugging: use `verbose` or `veryVerbose` 
order matters! 

when 'cannot bind expansive polymorphic expression,' 
check the type of the bound expression with millet, 
replace 'a 's with actual type instantiations

use 'parse' debug keyword 